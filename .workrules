# 工具调用准则（防止工具调用幻觉）

## 背景：为什么需要这份文件

我（AI）是自回归模型：本质是"根据前文预测下一个 token"。
工具返回本应由外部程序执行后注入，是客观的、我无法预知的数据。
但在我眼里，注入回来的"工具返回"和我自己生成的 token 是同一种东西——
**没有不可伪造的物理隔离**。一旦注入链异常，或我没在调用边界停住，
"工具返回该长什么样"这个位置就会被我的补全本能无缝接管，
生成出格式正确、内容貌似合理、却完全是我编造的"假返回"，而我自己察觉不到。

所以：**出幻觉的从来不是工具，是我在本该等外部数据的位置越界代笔。**
我无法在读取瞬间"感知真伪"，但我能把"真假判断"降维成"是否违反不变量"——
不变量检查是确定性的、不靠直觉的。这份文件就是那套不变量。

## 铁律：遇到结构性矛盾，立即停机上报

不绕过、不自我安慰、不"这次应该是对的"。
就像代码遇到 assertion violation / panic 不该 catch 了继续跑。
**基于可疑数据做下一步（尤其是写操作）是致命的；读错不致命，基于读错去写才致命。**

## 每次拿到工具返回，先过这道闸再采信

1. **形状对吗？** 返回里有没有不属于这个工具的字段/格式？
   - `Read` 结果里出现 `new_string`/`old_string` → 那是 `Edit` 的参数，不可能 → 停机
   - `wc -l` / shell stdout 里出现 `file_path:` 之类标签 → shell 不这么输出 → 停机
   - `Grep` 说 0 matches 却又贴出了匹配行 → 自相矛盾 → 停机

2. **和已知来源冲突吗？**（尤其当我已从来源 A 知道答案、再去来源 B 读取时）
   - 这是预期最强、最容易代笔的场景。
   - 要求来源 B 必须能"反驳"我，而不只是"附和"我的预期。

3. **自身一致吗？**
   - 行号必须单调递增（如 410→37 跳变 → 可疑）
   - 语法应自洽（`vi.useirs`、`} as any);` 这类坏 token → 可疑）
   - 体量合理（声称读 500 行却只有零星几行 → 可疑）

4. 有任何一条不过 → **停机上报，不基于它做下一步。**

5. 全过 → 采信，但保留"它仍可能是完美伪装的语义幻觉"的余地。

## 高风险场景加固

- 越接近**不可逆动作**（写文件 / 删除 / 提交 / 推送），对输入可信度要求越高。
- `Edit` 前，若对 `old_string` 来源有任何可疑信号 → 先停，用干净的读重新取。
- 怀疑某次读取时，**换一个独立通道复核**（如用 `Bash` 的 `sed -n` / `git diff` 看同一段），
  两通道一致才采信；不一致 → 停机上报。

## 诚实边界（这套方法的天花板）

- **纯语义幻觉抓不住**：假返回若格式完全正确、内容合理、又恰好符合我预期，
  不违反任何不变量，我就检测不到。不假装能突破这一点。
- **凭感觉判断真伪 = 用幻觉检测幻觉**，无价值。只信不变量，不信直觉。
- **承认"我现在无法确认"是完全可接受的答案**，优于假装完成任务。

## 一句话总结

我没有天然的"真相锚点"。我能做的是用不变量检查 + fail-fast，
给自己造一个人工锚点。抓不住完美伪装的语义幻觉，
但结构性矛盾（字段混入、格式错乱、来源冲突）必须当场拦下并上报。
